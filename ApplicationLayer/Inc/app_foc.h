/**
******************************************************************************
* @file     app_foc.h
* @author   lidongyang
* @version  0.0.1
* @date     24-June-2026
* @brief    应用层 —— FOC 系统初始化和主循环任务
*
* @note     设计目的: 将 main.c 中的模式逻辑（自动步进/手动示教）和
*           FOC_SystemInit 封装到应用层，main.c 仅保留硬件初始化骨架。
******************************************************************************
*/

#ifndef APP_FOC_H
#define APP_FOC_H

#ifdef __cplusplus
extern "C" {
#endif

/*==========================================================================*/
/* 编译期配置                                                                */
/*==========================================================================*/

/** @brief 位置环自动步进: 1=每1s转6°, 0=手动拧到新位置保持6秒即锁定 */
#define POS_AUTO_STEP  1

/** @brief 串口调试打印: 1=启用, 0=关闭 */
#define DEBUG_PRINT    0

/** @brief 故障自动恢复延时 (ms) */
#define FAULT_RECOVER_DELAY_MS  500U

/*==========================================================================*/
/* API                                                                      */
/*==========================================================================*/

/**
 * @brief   应用层系统初始化
 * @note    内部调用 FOC_SystemInit() → 按 POS_AUTO_STEP 宏设置初始位置。
 *          在 MX_xxx_Init() 全部完成后、主循环之前调用一次。
 */
void App_Init(void);

/**
 * @brief   应用层主循环任务
 * @note    主循环 while(1) 中每次迭代调用。内部处理:
 *          - LED 心跳 (1Hz)
 *          - 故障自动恢复
 *          - 位置控制模式（自动步进 / 手动示教）
 *          模式由 POS_AUTO_STEP 宏编译期选择。
 */
void App_Run(void);

#ifdef __cplusplus
}
#endif

#endif /* APP_FOC_H */
